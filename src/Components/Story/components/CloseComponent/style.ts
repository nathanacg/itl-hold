import styled from "styled-components/native";

export const CloseStory = styled.View`
    flex-direction: row;
    align-items: center;
    position: absolute;
    top: 15px;
    right: 0px;
    gap: 10px;
    z-index: 9999;
`