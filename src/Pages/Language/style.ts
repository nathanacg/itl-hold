import styled from 'styled-components/native';

export const TextContainerMark = styled.TouchableOpacity`
   flex-direction: row;
   justify-content: space-between;
   gap: 7px;
   align-items: center;
`;