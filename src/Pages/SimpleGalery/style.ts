import styled from "styled-components/native";

export const Header=styled.View`
    height: 65px;
    flex-direction: row;
    align-items:center;
    justify-content: flex-end;
    padding:0 20px;
    top:0;
`
