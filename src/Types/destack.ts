export interface Destacks {
    destackId: number,
    title: string,
    imageDestack: string,
    storiesArchiveds: string,
    userId: number
}

export interface Highlights {
    id: number,
    userId: number,
    title: string,
    coverUrl: string
}